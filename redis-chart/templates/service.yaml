apiVersion: v1
kind: Service
metadata:
  name: redis-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: redis
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 6379
